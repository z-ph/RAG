spring:
  datasource:
    url: jdbc:h2:mem:knowledge-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

qdrant:
  create-collection-if-not-exists: false

rag:
  chunk-dedup-enabled: false

auth:
  bootstrap-admin:
    username: admin
    password: ChangeMe123!
