openapi: 3.0.3
info:
  title: Knowledge RAG API
  version: 1.2.0
  description: |
    基于当前代码实现整理的接口规范，覆盖登录鉴权、注册码管理、文档上传管理、公开文档浏览与 RAG 问答接口。

    说明：
    - 文档管理接口使用 Session Cookie 鉴权，登录成功后服务端会写入 `JSESSIONID`。
    - 公开文档接口（`/documents/public/**`）无需鉴权，所有限流保护。
    - RAG 问答接口保持匿名可访问。
    - `/auth/register` 只能使用管理员创建的一次性注册码注册。
servers:
  - url: http://localhost:8080
    description: Local development
tags:
  - name: Auth
    description: 登录、注册、当前会话与注册码管理
  - name: Documents
    description: 文档上传、列表查询、删除和健康检查
  - name: Public Documents
    description: 公开文档浏览与下载（无需鉴权）
  - name: Admin Documents
    description: 管理员文档维护接口
  - name: RAG
    description: 同步问答、流式问答、会话控制和健康检查
paths:
  /auth/login:
    post:
      tags:
        - Auth
      summary: 登录
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
            example:
              username: admin
              password: ChangeMe123!
      responses:
        '200':
          description: 登录成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSuccessResponse'
              example:
                message: 登录成功
                user:
                  username: admin
                  role: ADMIN
        '400':
          description: 参数不合法
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: 用户名或密码错误
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/register:
    post:
      tags:
        - Auth
      summary: 使用注册码注册
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
            example:
              username: editor01
              password: Password123
              registrationCode: ABCD-EFGH-JKLM
      responses:
        '201':
          description: 注册成功，并自动登录
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSuccessResponse'
              example:
                message: 注册成功
                user:
                  username: editor01
                  role: USER
        '400':
          description: 用户名冲突、注册码无效、已使用、已禁用或已过期
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/logout:
    post:
      tags:
        - Auth
      summary: 退出登录
      operationId: logout
      responses:
        '200':
          description: 已退出登录
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
              example:
                message: 已退出登录
  /auth/me:
    get:
      tags:
        - Auth
      summary: 查看当前登录状态
      operationId: currentAuthStatus
      responses:
        '200':
          description: 当前会话状态
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthStatusResponse'
              examples:
                anonymous:
                  value:
                    authenticated: false
                    user: null
                authenticated:
                  value:
                    authenticated: true
                    user:
                      username: admin
                      role: ADMIN
  /auth/registration-codes:
    get:
      tags:
        - Auth
      summary: 管理员查看注册码列表
      operationId: listRegistrationCodes
      security:
        - cookieAuth: []
      responses:
        '200':
          description: 注册码列表
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistrationCodeListResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
    post:
      tags:
        - Auth
      summary: 管理员创建注册码
      operationId: createRegistrationCode
      security:
        - cookieAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegistrationCodeCreateRequest'
            example:
              note: 运营同事
              expiresAt: '2026-04-01T18:30:00'
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistrationCodeResponse'
        '400':
          description: 到期时间不合法等参数错误
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
  /auth/registration-codes/{id}/disable:
    patch:
      tags:
        - Auth
      summary: 管理员禁用注册码
      operationId: disableRegistrationCode
      security:
        - cookieAuth: []
      parameters:
        - $ref: '#/components/parameters/RegistrationCodeId'
      responses:
        '200':
          description: 禁用成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistrationCodeResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: 注册码不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/registration-codes/{id}:
    delete:
      tags:
        - Auth
      summary: 管理员删除注册码
      operationId: deleteRegistrationCode
      security:
        - cookieAuth: []
      parameters:
        - $ref: '#/components/parameters/RegistrationCodeId'
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
              example:
                message: 注册码已删除
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: 注册码不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /documents/upload:
    post:
      tags:
        - Documents
      summary: 上传并处理文档
      operationId: uploadDocument
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: 支持 `.pdf`、`.txt`、`.doc`、`.docx`
      responses:
        '200':
          description: 文档处理成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentResponse'
        '400':
          description: 文件为空、文件名缺失或文件格式不支持
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          description: 文档解析、切块或向量入库失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /documents:
    get:
      tags:
        - Documents
      summary: 获取已上传文档列表
      operationId: listDocuments
      security:
        - cookieAuth: []
      responses:
        '200':
          description: 文档列表
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentListResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '500':
          description: 查询失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /documents/{documentId}:
    delete:
      tags:
        - Documents
      summary: 删除指定文档
      operationId: deleteDocument
      security:
        - cookieAuth: []
      parameters:
        - $ref: '#/components/parameters/DocumentId'
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentDeleteResponse'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          description: 未找到对应文档
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 删除失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /documents/public:
    get:
      tags:
        - Public Documents
      summary: 获取公开文档列表
      operationId: listPublicDocuments
      description: 返回所有文档的元数据列表，无需鉴权。受 IP 限流保护（30 次/分钟）。
      responses:
        '200':
          description: 文档列表
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicDocumentListResponse'
        '429':
          description: 请求过于频繁
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 查询失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /documents/public/{documentId}:
    get:
      tags:
        - Public Documents
      summary: 获取公开文档详情
      operationId: getPublicDocumentDetail
      description: 返回文档元数据及全部文本段落，无需鉴权。受 IP 限流保护（30 次/分钟）。
      parameters:
        - $ref: '#/components/parameters/DocumentId'
      responses:
        '200':
          description: 文档详情
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicDocumentDetailResponse'
        '404':
          description: 文档不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '429':
          description: 请求过于频繁
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 查询失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /documents/public/{documentId}/download:
    get:
      tags:
        - Public Documents
      summary: 下载文档原文件
      operationId: downloadPublicDocument
      description: 下载文档原始文件（如 PDF、TXT、DOC、DOCX），无需鉴权。受 IP 限流保护（10 次/分钟）。
      parameters:
        - $ref: '#/components/parameters/DocumentId'
      responses:
        '200':
          description: 文件二进制流
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            text/plain:
              schema:
                type: string
                format: binary
        '404':
          description: 文档或文件不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '429':
          description: 请求过于频繁
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 下载失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /documents/health:
    get:
      tags:
        - Documents
      summary: 文档服务健康检查
      operationId: documentHealth
      responses:
        '200':
          description: 服务正常
          content:
            text/plain:
              schema:
                type: string
              example: 文档服务运行正常
  /documents/images/{documentId}/{imageId}:
    get:
      tags:
        - Public Documents
      summary: 获取文档内嵌图片
      operationId: getDocumentImage
      description: 公开返回文档解析时抽取出的内嵌图片，无需鉴权。图片正文中推荐使用该规范路径；服务端仍兼容旧的 `/rag/documents/images/**` 前缀。
      parameters:
        - $ref: '#/components/parameters/DocumentId'
        - name: imageId
          in: path
          required: true
          description: 图片文件名，包含扩展名，例如 `f69ed81fbb6a40c1.png`
          schema:
            type: string
      responses:
        '200':
          description: 图片二进制流
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/gif:
              schema:
                type: string
                format: binary
            image/bmp:
              schema:
                type: string
                format: binary
        '403':
          description: 图片路径非法
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: 图片不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /admin/documents/{documentId}/reindex:
    post:
      tags:
        - Admin Documents
      summary: 重建指定文档索引
      operationId: reindexDocument
      description: 仅管理员可调用。服务会读取原始文件，按当前切块策略重新生成片段并覆盖旧向量；当旧片段已丢失但原始文件仍在时，也可按原 documentId 恢复。
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/DocumentId'
      responses:
        '200':
          description: 重建完成
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  documentId:
                    type: string
                  deletedSegments:
                    type: integer
                  newSegments:
                    type: integer
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '403':
          $ref: '#/components/responses/ForbiddenError'
        '404':
          description: 原始文件不存在或文档无法恢复
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /rag/ask:
    post:
      tags:
        - RAG
      summary: 同步 RAG 问答
      operationId: askRag
      description: |
        同步返回最终答案与来源片段。

        注意：
        - `conversationId` 可选。
        - 如果未传 `conversationId`，服务不会自动生成会话 ID，响应中的 `conversationId` 也会是 `null`。
        - `maxResults` 的最终生效值不会超过服务端配置 `rag.max-results`。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RagRequest'
            example:
              question: 信访受理流程是什么？
              conversationId: case-001
              maxResults: 5
      responses:
        '200':
          description: 问答成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RagResponse'
        '400':
          description: 请求无效，例如问题为空
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 问答处理失败
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /rag/ask/stream:
    post:
      tags:
        - RAG
      summary: 流式 RAG 问答
      operationId: askRagStream
      description: |
        使用 `text/event-stream` 持续推送回答过程。

        典型事件顺序：
        - `start`
        - `sources`
        - `thinking_delta`
        - `thinking_end`
        - `delta`
        - `complete`
        - `cancelled`
        - `error`
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RagRequest'
      responses:
        '200':
          description: SSE 事件流
          content:
            text/event-stream:
              schema:
                type: string
        '400':
          description: 请求无效，例如问题为空
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: 建流前即失败时返回；建流后的错误通常以 SSE `error` 事件推送
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /rag/conversations/{conversationId}/cancel:
    post:
      tags:
        - RAG
      summary: 取消指定会话的进行中生成任务
      operationId: cancelConversationGeneration
      parameters:
        - $ref: '#/components/parameters/ConversationId'
      responses:
        '200':
          description: 取消成功
          content:
            text/plain:
              schema:
                type: string
              example: 已取消该会话的进行中生成任务
        '404':
          description: 当前会话没有进行中的生成任务
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /rag/conversations/{conversationId}:
    delete:
      tags:
        - RAG
      summary: 清空指定会话上下文
      operationId: clearConversation
      parameters:
        - $ref: '#/components/parameters/ConversationId'
      responses:
        '200':
          description: 清理成功
          content:
            text/plain:
              schema:
                type: string
              example: 会话上下文已清空
  /rag/health:
    get:
      tags:
        - RAG
      summary: RAG 服务健康检查
      operationId: ragHealth
      responses:
        '200':
          description: 服务正常
          content:
            text/plain:
              schema:
                type: string
              example: RAG 服务运行正常
    post:
      tags:
        - RAG
      summary: RAG 服务健康检查（POST）
      operationId: ragHealthPost
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HealthCheckRequest'
            example:
              echo: ping
      responses:
        '200':
          description: 服务正常
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthCheckResponse'
              example:
                status: UP
                message: RAG 服务运行正常
                echo: ping
                timestamp: '2026-04-29T12:00:00'
components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: JSESSIONID
  responses:
    UnauthorizedError:
      description: 未登录
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: 未登录
            message: 请先登录后再访问该接口
            timestamp: '2026-03-29T18:00:00'
    ForbiddenError:
      description: 无权限
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: 无权限
            message: 当前账号无权执行该操作
            timestamp: '2026-03-29T18:00:00'
  parameters:
    DocumentId:
      name: documentId
      in: path
      required: true
      description: 文档唯一标识
      schema:
        type: string
    ConversationId:
      name: conversationId
      in: path
      required: true
      description: 会话唯一标识
      schema:
        type: string
    RegistrationCodeId:
      name: id
      in: path
      required: true
      description: 注册码主键 ID
      schema:
        type: integer
        format: int64
  schemas:
    LoginRequest:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
        password:
          type: string
    RegisterRequest:
      type: object
      required:
        - username
        - password
        - registrationCode
      properties:
        username:
          type: string
        password:
          type: string
        registrationCode:
          type: string
    AuthUserResponse:
      type: object
      required:
        - username
        - role
      properties:
        username:
          type: string
        role:
          type: string
          example: ADMIN
    AuthSuccessResponse:
      type: object
      required:
        - message
        - user
      properties:
        message:
          type: string
        user:
          $ref: '#/components/schemas/AuthUserResponse'
    AuthStatusResponse:
      type: object
      required:
        - authenticated
      properties:
        authenticated:
          type: boolean
        user:
          allOf:
            - $ref: '#/components/schemas/AuthUserResponse'
          nullable: true
    RegistrationCodeCreateRequest:
      type: object
      properties:
        note:
          type: string
          nullable: true
        expiresAt:
          type: string
          format: date-time
          nullable: true
    RegistrationCodeListResponse:
      type: object
      required:
        - codes
        - total
      properties:
        codes:
          type: array
          items:
            $ref: '#/components/schemas/RegistrationCodeResponse'
        total:
          type: integer
          format: int32
    RegistrationCodeResponse:
      type: object
      required:
        - id
        - code
        - createdBy
        - createdAt
        - status
      properties:
        id:
          type: integer
          format: int64
        code:
          type: string
        note:
          type: string
          nullable: true
        createdBy:
          type: string
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
          nullable: true
        usedAt:
          type: string
          format: date-time
          nullable: true
        usedBy:
          type: string
          nullable: true
        disabledAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [AVAILABLE, USED, DISABLED, EXPIRED]
    MessageResponse:
      type: object
      required:
        - message
      properties:
        message:
          type: string
    RagRequest:
      type: object
      required:
        - question
      properties:
        question:
          type: string
          minLength: 1
        conversationId:
          type: string
          nullable: true
        maxResults:
          type: integer
          nullable: true
          minimum: 1
    RagResponse:
      type: object
      required:
        - answer
        - sources
      properties:
        answer:
          type: string
        thinking:
          type: string
          nullable: true
        conversationId:
          type: string
          nullable: true
        sources:
          type: array
          items:
            $ref: '#/components/schemas/SourceReference'
    SourceReference:
      type: object
      required:
        - filename
        - excerpt
        - relevanceScore
      properties:
        filename:
          type: string
        excerpt:
          type: string
        relevanceScore:
          type: number
          format: double
    DocumentResponse:
      type: object
      required:
        - message
        - segmentCount
      properties:
        documentId:
          type: string
          nullable: true
        filename:
          type: string
          nullable: true
        message:
          type: string
        segmentCount:
          type: integer
          format: int32
          minimum: 0
    DocumentListResponse:
      type: object
      required:
        - documents
        - total
      properties:
        documents:
          type: array
          items:
            $ref: '#/components/schemas/DocumentListItemResponse'
        total:
          type: integer
          format: int32
          minimum: 0
    DocumentListItemResponse:
      type: object
      required:
        - documentId
        - filename
        - segmentCount
      properties:
        documentId:
          type: string
        filename:
          type: string
        segmentCount:
          type: integer
          format: int32
          minimum: 0
    DocumentDeleteResponse:
      type: object
      required:
        - documentId
        - deletedSegments
        - message
      properties:
        documentId:
          type: string
        filename:
          type: string
          nullable: true
        deletedSegments:
          type: integer
          format: int32
          minimum: 0
        message:
          type: string
    PublicDocumentSegment:
      type: object
      required:
        - chunkIndex
        - text
      properties:
        chunkIndex:
          type: integer
          format: int32
        text:
          type: string
    PublicDocumentListItem:
      type: object
      required:
        - documentId
        - filename
        - title
        - category
        - documentTime
        - keywords
        - segmentCount
      properties:
        documentId:
          type: string
        filename:
          type: string
        title:
          type: string
        category:
          type: string
        documentTime:
          type: string
        keywords:
          type: string
        segmentCount:
          type: integer
          format: int32
          minimum: 0
    PublicDocumentListResponse:
      type: object
      required:
        - documents
        - total
      properties:
        documents:
          type: array
          items:
            $ref: '#/components/schemas/PublicDocumentListItem'
        total:
          type: integer
          format: int32
          minimum: 0
    PublicDocumentDetailResponse:
      type: object
      required:
        - documentId
        - filename
        - title
        - category
        - documentTime
        - keywords
        - segmentCount
        - segments
      properties:
        documentId:
          type: string
        filename:
          type: string
        title:
          type: string
        category:
          type: string
        documentTime:
          type: string
        keywords:
          type: string
        segmentCount:
          type: integer
          format: int32
          minimum: 0
        segments:
          type: array
          items:
            $ref: '#/components/schemas/PublicDocumentSegment'
    ErrorResponse:
      type: object
      required:
        - error
        - message
        - timestamp
      properties:
        error:
          type: string
        message:
          type: string
        timestamp:
          type: string
          format: date-time
    HealthCheckRequest:
      type: object
      properties:
        echo:
          type: string
          nullable: true
    HealthCheckResponse:
      type: object
      required:
        - status
        - message
        - timestamp
      properties:
        status:
          type: string
          example: UP
        message:
          type: string
        echo:
          type: string
          nullable: true
        timestamp:
          type: string
          format: date-time
